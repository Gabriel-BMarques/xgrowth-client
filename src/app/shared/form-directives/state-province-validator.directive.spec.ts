import { StateProvinceValidatorDirective } from './state-province-validator.directive';

describe('StateProvinceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StateProvinceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
